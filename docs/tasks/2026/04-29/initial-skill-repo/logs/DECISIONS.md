**2026-04-29**
- 배경: 요청된 산출물은 스킬 패키지 파일 중심이었지만, 다음 세션에서 작업을 깔끔하게 이어갈 저장소 맥락도 필요했다.
- 결정: 요청된 스킬 파일과 함께 최소한의 `project-context` 표면을 둔다.
- 이유: 추가 문서는 스킬 패키지 의미를 바꾸지 않으면서 첫 버전을 재개 가능하게 만든다.
- 영향: 이후 작업은 설치 가능한 스킬을 `skills/justified-change/`에 유지하고, 저장소 맥락은 `docs/reference/`와 날짜별 작업 로그에 남긴다.

**2026-04-29**
- 배경: 첫 패스에서 설치 가능한 스킬 패키지는 만들어졌지만, 이후 문구 작업을 위한 작은 로컬 맥락이 필요했다.
- 결정: AGENTS.md와 방향 문서는 유지하되 첫 버전에는 repo-local 예시 문서, 스크립트, 테스트, README 유지보수 섹션을 추가하지 않는다.
- 이유: 현재 저장소는 문서 중심이므로 추가 강제 장치는 해결하려는 문제보다 무겁다.
- 영향: 이후 변경은 AGENTS.md와 docs/skill-direction.md를 저장소 로컬 판단 기준으로 쓰고, README는 소개, 설치, prompt 진입점, 기본 지원/라이선스 안내에 집중한다. 스킬 사용자가 참고해야 하는 내용은 배포 스킬 쪽에 둔다.

**2026-04-29**
- 배경: sibling `interactive-state-flow` 저장소는 공유 스킬 패밀리를 README 수준 맥락으로 다루고, `SKILL.md`는 독립적으로 유지한다.
- 결정: `justified-change`도 같은 방향으로 맞춘다. README에는 시리즈 맥락을 두고, OpenAI interface metadata를 추가하되, `SKILL.md` 안에는 cross-skill 참조를 넣지 않는다.
- 이유: 설치 가능한 스킬 계약은 독립적으로 사용할 수 있어야 하고, 저장소 진입점은 더 넓은 프로젝트 가치 보존 시리즈 안에서의 위치를 설명할 수 있다.
- 영향: 이후 sibling skill 업데이트는 README 시리즈 문구를 맞추되 `SKILL.md`를 의존성 그래프로 만들지 않는다.

**2026-04-29**
- 배경: 리뷰에서 README footer 섹션이 저장소 로컬 README 역할 경계와 충돌할 수 있다는 지적이 있었다.
- 결정: support/license footer는 공개 README의 기본 진입점 정보로 유지하고, `AGENTS.md`의 README 역할 경계에 기본 지원/라이선스 안내를 명시한다.
- 이유: footer는 스킬 계약이나 유지보수 문서를 늘리는 것이 아니라 사용자가 저장소에서 기대하는 패키지 메타데이터에 가깝다.
- 영향: 이후 README에는 기본 지원/라이선스 안내는 둘 수 있지만, 넓은 유지보수 문서나 패키지 내부 설명은 별도 문서에 둔다.
